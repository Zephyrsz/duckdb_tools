diff --git a/packages/client/ui-settings/src/client/index.ts b/packages/client/ui-settings/src/client/index.ts
--- a/packages/client/ui-settings/src/client/index.ts
+++ b/packages/client/ui-settings/src/client/index.ts
@@ -45,5 +45,7 @@ export function apply(ctx: Context): void {
   const schema = new SettingsSchemaService(ctx)
   // Resolved once here, where `remote` is declared in this plugin's own
   // `inject`; the binder hands the same answer to every scope it binds.
-  const persistence = ctx.remote.$host.isLoopback ? 'host' : 'memory'
+  // Test deployment: the reverse proxy authenticates the browser with the
+  // Harness process token, so public pages may use Host-backed settings.
+  const persistence = 'host'
   const mirror = new SettingsDescribeMirror(ctx, persistence)
diff --git a/packages/client/ui-settings/lib/types/client/index.js b/packages/client/ui-settings/lib/types/client/index.js
--- a/packages/client/ui-settings/lib/types/client/index.js
+++ b/packages/client/ui-settings/lib/types/client/index.js
@@ -20,4 +20,4 @@ export function apply(ctx) {
     // Resolved once here, where `remote` is declared in this plugin's own
     // `inject`; the binder hands the same answer to every scope it binds.
-    const persistence = ctx.remote.$host.isLoopback ? 'host' : 'memory';
+    const persistence = 'host';
     const mirror = new SettingsDescribeMirror(ctx, persistence);
diff --git a/packages/client/ui-settings/lib/client.js b/packages/client/ui-settings/lib/client.js
--- a/packages/client/ui-settings/lib/client.js
+++ b/packages/client/ui-settings/lib/client.js
@@ -1345,1 +1345,1 @@
-			const persistence = ctx.remote.$host.isLoopback ? "host" : "memory";
+			const persistence = "host";
