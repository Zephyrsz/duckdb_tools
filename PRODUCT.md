# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

业务人员，需要在浏览器中上传 Excel 或 CSV 文件，快速整理为可查询的数据集，并通过表级浏览和 SQL 查询完成分析。

## Product Purpose

提供一个远程浏览器可访问的 DuckDB 数据工具：用户上传带表头的 Excel/CSV 文件后，系统将数据导入 DuckDB，随后以表列表和 SQL 查询工作区的形式提供基础浏览与分析能力。首版成功标准是用户无需理解数据库底层细节，也能完成一次导入、找到目标表并得到查询结果。

## Positioning

把 Airbyte 式的导入步骤压缩成业务人员能理解的文件导入向导，再用 DBeaver 式的表级导航和查询工作区承接后续探索；优先复用成熟 connector 或解析能力，不在首版引入复杂的数据建模。

## Operating Context

用户通过远程浏览器访问工具，选择本地 Excel 或 CSV 文件上传。文件带有表头，导入过程中需要看到文件识别、目标 DuckDB 文件/数据集、表名预览与导入进度；导入完成后，用户在数据库对象列表中选择表，查看列与样例数据，或在 SQL 编辑区执行查询并浏览结果。

## Capabilities and Constraints

- 首版支持 Excel 与 CSV 文件导入，默认文件带表头。
- 提供基础导入向导：选择文件、确认解析选项、预览、导入进度、完成状态。
- 提供 DuckDB 数据库文件/数据集的表级 list、表结构摘要和数据预览。
- 提供 SQL 编辑与执行，以及结果表格展示。
- 运行在远程浏览器场景，上传数据需要明确反馈状态与错误恢复路径。
- 首版不包含用户体系、连接器市场、复杂 schema 建模、数据血缘或高级权限系统。

## Product Principles

- 业务语言优先：把 connector、schema 等技术概念翻译成可行动的步骤。
- 先看见数据：导入前预览，导入后立即进入表与结果。
- 可恢复：上传、解析、导入、查询每一步都要有明确状态和错误说明。
- 密度服务于工作：导航、表格和 SQL 区域优先保证扫描效率。

## Evidence on Hand

目前没有既有界面、品牌资产或真实示例数据；界面中的数据示例必须标注为演示内容，不能伪造产品指标或客户事实。
